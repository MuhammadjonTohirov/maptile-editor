FROM pgrouting/pgrouting:15-3.5-4.0.1

# PostgreSQL stays on major 15 so the existing data directory remains binary
# compatible. The complete image tag pins PostGIS and pgRouting as required by
# architecture rule X4; migrations upgrade the extension catalog to 4.0.1.
